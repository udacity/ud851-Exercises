<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- COMPLETED(2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- COMPLETED (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="@bool/pref_default_value"
        android:key="@string/pref_show_bass_key"
        android:summaryOff="@string/pref_show_bass_hidden"
        android:summaryOn="@string/pref_show_bass_shown"
        android:title="@string/pref_show_bass_title" />

</PreferenceScreen>
